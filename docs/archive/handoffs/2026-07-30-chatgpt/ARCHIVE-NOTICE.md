# ChatGPT Handoff Archive Notice

This directory preserves the original Chinese-language MeterDesk hardening handoff generated on
2026-07-30. It is historical input, not an authoritative specification or an executable plan.

The package was reviewed against the local repository before integration. Durable decisions were
rewritten in English in the active hardening roadmap, P0-01 feature spec, engineering evidence
matrix, and foundational specs. The archived implementation plan is explicitly stale.

Windows `Zone.Identifier` download metadata was removed during archival. `FILE-INDEX.json` retains
the package author's original file index and byte counts; those counts predate the archive notices.
Five Markdown hard breaks were normalized from trailing spaces to explicit `<br>` elements so the
archive passes repository whitespace checks without changing the rendered meaning.

Start from the repository [documentation index](../../../README.md) and do not copy this archive's
nested `docs/` tree back into the repository root.
